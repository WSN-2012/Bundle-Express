<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="se.kth.ssvl.tslab.wsn"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="15" />

    <!-- Permission - Internet Connect -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Network State Permissions -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name=".app.MainActivity"
            android:label="@string/title_activity_main" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".app.WebData"
            android:label="@string/title_activity_web_data" >
        </activity>
        <activity
            android:name=".app.DaemonConfig"
            android:label="@string/title_activity_daemon_config" >
        </activity>
        <activity
            android:name=".app.BundleStatistics"
            android:label="@string/title_activity_bundle_statistics" >
        </activity>
        <activity
            android:name=".app.WebDataContent"
            android:label="@string/title_activity_web_data_content" >
        </activity>
        <activity
            android:name=".app.SensorList"
            android:label="@string/title_activity_sensor_list" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <service android:enabled="true" android:name=".WSNService"/>
    </application>

</manifest>